# 0.1.1 修复验证

日期：2026-09-24。环境：Linux / Node.js 22 / Chromium / Playwright。

## 本地结果

- `npm run check`、`npm test`（29 项）与 `npm run build` 通过。
- `scripts/browser-smoke.py` 和 `scripts/browser-interaction.py` 通过：原有编辑、描摹、保存及 SVG 导出没有测试回归。
- `scripts/browser-preview-cut.py` 通过：实际拖动阈值滑块、快速连改参数、取消、原图/轮廓/矢量切换、预览与应用路径相同、一次撤销、去杂点、尺寸参数、关闭自动预览后的手工生成、替换项目、有限线段切割、未触及对象逐字段不变、端点及撤销。
- 13 项新增几何回归包括：未相交延长线、不完整切口、内点到内点、边界端点、反向拖动、切线、顶点穿越、复合对象、凹形局部切割、圆环/孔洞、复杂度预算、斜向共边精度，以及 900 个固定种子的面积守恒样本。

## 范围和限制

- 浏览器回归使用真实 Blob Worker 和打包 HTML；不是 Windows 实机或系统剪贴板权限测试。
- 当前环境拦截了浏览器对本地开发服务器的导航（ERR_BLOCKED_BY_ADMINISTRATOR），因此未通过这个入口验证原始 ES 模块加载；不得将单文件测试写成开发服务器测试。
- GitHub Pages 的实际发布状态以对应 Actions 运行和公开页面验证为准。
- 180 毫秒是输入防抖时间，不是承诺的计算完成时间；预览不保证 60 fps。
- 描摹仍为原有像素轮廓预览算法，未换成 VTracer。切割仍用 0.35 px 折线近似，不是无损贝塞尔切割。
- PSD、AI、CDR 和专色导出不在此次改动范围内。
