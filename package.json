{
  "name": "vector-studio",
  "version": "0.1.1",
  "private": true,
  "description": "Local-first raster tracing and SVG path editor. No server-side processing.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/geometry.js && node --check src/trace.js && node --check src/trace-worker.js && node --check src/trace-preview.js",
    "build": "node scripts/build.mjs"
  }
}
