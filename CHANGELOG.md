# Changelog

## 0.2.1 — 2026-09-24

- 图形属性新增六向对齐；基准可选所选整体边界或画板。使用三次贝塞尔实际路径极值，不按控制多边形，也不计描边。
- 新增水平／垂直均匀分布，可选边缘等间距或中心等距；至少三个单位，首尾固定。空间不足时不强制负间距。
- 完整选中群组作为一个单位移动；部分选中只移动所选成员。平移不改曲线手柄、不合并几何，一次撤销恢复。
- 节点拖动和钢笔落点新增 8 屏幕像素吸附、十字提示、全部节点／仅开放端点选项。Shift 在拖动途中或钢笔落点时临时关闭；不会自动连接路径。
- 增加精确匹配填充色／描边色的选择工具及右键入口，跳过隐藏／锁定对象，不选入群组内其他颜色，不修改项目。
- 修复仅单击选择后按钮仍灰的问题；拖动取消、Esc、失去捕获与窗口焦点时还原未提交手势并清除吸附提示。
- 新增 19 项几何／状态回归和浏览器实际操作回归；加入 GitHub Actions 部署前检查。
- 本次基于已发布的 0.2.0 主线。此前单独提供的 0.3.0 曲线实验 HTML 未合入仓库；本次不宣称实现新的自动贝塞尔描摹选项。

## 0.2.0 — 2026-09-24

- 新增同浏览器项目库、缩略图、搜索、保存/另存/删除/备份；切换时保存/放弃/取消；跨标签页 revision 防覆盖。
- 无矢量、无选择及运算中禁用不适用按钮；预览不作为正式矢量。
- 双击画布图形进入节点编辑；Ctrl/Command+点击路径加点；Alt+点击节点删点；保留最小节点数。
- 图层显示真实形状缩略图及群组树；新增嵌套群组/解组、整体移动/复制/排序，几何不融合。
- 新增按上下文启用的右键菜单，包含节点、群组、布尔、叠放、显隐、锁定和项目入口。
- 项目 version 2 支持群组并迁移 version 1；SVG 导出真实嵌套 g。
- 新增受限实验 PSD 导出：纯色闭合形状、矢量蒙版、群组、RGBA 预览；不支持描边/开放路径/印刷色彩，未完成 Photoshop 应用验收。
- 原生 AI/CDR 尚未实现，导出面板明确禁用而不是改名冒充。
- 新增模型/格式及浏览器工作区回归、CI 真实源存储测试和独立 PSD 解析检查。

## 0.1.1 — 2026-09-24

- 图片导入后自动生成临时预览；阈值、简化容差、去杂点、颜色数量及描摹尺寸变化后自动刷新。
- 增加原图叠加轮廓、矢量填色、原图对照及取消预览。预览不写项目、图层或撤销历史。
- 后台 Worker 描摹以最新参数为准，取消旧任务；预览和确认生成使用相同尺寸及精度。
- 小刀改为鼠标按下到松开之间的有限线段；不再延长切到其他对象或凹形的远端。
- 保留不相交对象、真实孔洞、撤销；增加起终点标记和未贯穿区域的提示。
- 新增 13 项几何回归（含 900 个确定性样本）及预览/切割浏览器回归脚本。

## 0.1.0 — 2026-09-23

- 首个原生 SVG / JavaScript 功能预览版，无 npm 运行依赖。
- 黑白与少色描摹、节点/手柄、绘图、直线切割、路径剪断/连接、折线近似布尔运算。
- 图层式对象管理、填色、撤销重做、项目保存及 SVG 输出。
- 模块化源代码与带 CSP 哈希的单文件 HTML 构建。
- Node 单元测试与浏览器回归脚本；GitHub Pages 自动发布配置。
- 当前不含 VTracer、PSD、原生 AI/CDR、CMYK/专色和通用 SVG 导入。
- 发布状态：源码包已生成；远程仓库与实际网站尚未创建。
